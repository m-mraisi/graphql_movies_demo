import { movieResolver } from './movie.resolver';
import { reviewResolver } from './review.resolver';
import { userResolver } from './user.resolver';

export default [userResolver, movieResolver, reviewResolver];
